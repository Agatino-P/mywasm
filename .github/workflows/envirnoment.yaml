jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: TestEnvironment        # <-- targeting this environment is what scopes the lookup
    steps:
      - name: Show the environment-scoped var
        run: echo "Value: ${{ vars.ENVIRONMENT_VAR }}"
